#include<stdio.h>
#include<map>
#include<string>
#include<iostream>
int main(){
  using namespace std;
  map<string,int> M;
  string t;
  while(cin>>t)
    M[t]++;
  for(map<string,int>::iterator j=M.begin();j!=M.end();++j)
    cout<<j->first<<"\t"<<j->second<<"\n";
  return 0;
}
