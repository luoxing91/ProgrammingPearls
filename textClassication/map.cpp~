#include<map>
int main(){
    string S="text text fg";
    istringstream ifs(C);
    string str;
    map<string,int> M;
    while(ifs>>str){
	M[str]++;
    }
    for(auto &x : C){
	cout<<x.first<<"\t"<<x.second<<endl;
    }
}
